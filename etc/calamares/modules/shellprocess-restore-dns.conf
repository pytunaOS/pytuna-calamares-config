dontChroot: true
timeout: 10
verbose: true

script:
  - "rm -f ${ROOT}/etc/resolv.conf"
  - "ln -s /run/systemd/resolve/stub-resolv.conf ${ROOT}/etc/resolv.conf"

i18n:
  name: "Finishing network configuration"
